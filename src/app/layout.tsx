import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import './globals.css';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'Tasker - Your Personal Task Manager',
  description: 'A powerful TickTick clone built with Next.js, TypeScript, and Tailwind CSS',
  keywords: ['task manager', 'todo', 'productivity', 'ticktick clone'],
  authors: [{ name: 'Tasker Team' }],
  viewport: 'width=device-width, initial-scale=1',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en" suppressHydrationWarning>
      <body className={inter.className} suppressHydrationWarning>
        {children}
      </body>
    </html>
  );
}